# Agent Memory

## 当前已确认规则

- `Reaction diffusion/RDcore/` 是外层仓库里可继续开发并可推送的目录。
- `Reaction diffusion/Yeast/` 只保留本地，不纳入外层仓库推送范围。
- `external/Yeast_closed_loop_DMD_automation` 是独立仓库；默认不由我执行提交、推送、合并或改写历史。

## 使用方式

- 新的稳定约束写进这里。
- 短期事项先写到 `00 Inbox.md`，整理后再归档。
- 项目专项信息放到 `10 Projects/` 或 `20 Knowledge/`。
